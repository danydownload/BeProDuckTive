package com.example.beproducktive.ui.viewpage

import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.example.beproducktive.R
import com.example.beproducktive.databinding.FragmentTasksBinding

/*

class ViewPageViewHolder(

    private val binding: FragmentTasksBinding

) : RecyclerView.ViewHolder(binding.root)
{

    fun bind() {

    }


    companion object {
        fun create(parent: ViewGroup) : ViewPageViewHolder {
            val view = LayoutInflater.from(parent.context)
                .inflate(R.layout.fragment_tasks, parent, false)
            val binding = FragmentTasksBinding.bind(view)
            return ViewPageViewHolder(binding)
        }
    }

}


 */